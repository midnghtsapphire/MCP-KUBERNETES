apiVersion: v1
kind: Service
metadata:
  name: mct-branding
  namespace: inthewild
  labels:
    app: mct-branding
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3007
    protocol: TCP
    name: http
  selector:
    app: mct-branding
---
