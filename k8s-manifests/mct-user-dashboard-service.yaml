apiVersion: v1
kind: Service
metadata:
  name: mct-user-dashboard
  namespace: inthewild
  labels:
    app: mct-user-dashboard
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: mct-user-dashboard
---
