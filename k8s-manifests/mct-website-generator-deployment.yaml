apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-website-generator
  namespace: inthewild
  labels:
    app: mct-website-generator
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-website-generator
  template:
    metadata:
      labels:
        app: mct-website-generator
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-website-generator
        image: ghcr.io/midnghtsapphire/mct-website-generator:latest
        ports:
        - containerPort: 3010
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3010"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 10
          periodSeconds: 5
---
