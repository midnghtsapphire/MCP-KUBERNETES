apiVersion: v1
kind: Service
metadata:
  name: mct-software-discovery
  namespace: inthewild
  labels:
    app: mct-software-discovery
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3014
    protocol: TCP
    name: http
  selector:
    app: mct-software-discovery
---
