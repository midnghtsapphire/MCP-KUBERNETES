apiVersion: v1
kind: Service
metadata:
  name: mct-analytics
  namespace: inthewild
  labels:
    app: mct-analytics
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3018
    protocol: TCP
    name: http
  selector:
    app: mct-analytics
---
