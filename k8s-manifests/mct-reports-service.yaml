apiVersion: v1
kind: Service
metadata:
  name: mct-reports
  namespace: inthewild
  labels:
    app: mct-reports
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3012
    protocol: TCP
    name: http
  selector:
    app: mct-reports
---
