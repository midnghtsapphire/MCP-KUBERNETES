apiVersion: v1
kind: Service
metadata:
  name: mct-auth
  namespace: inthewild
  labels:
    app: mct-auth
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: mct-auth
---
