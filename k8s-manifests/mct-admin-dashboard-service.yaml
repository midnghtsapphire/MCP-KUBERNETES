apiVersion: v1
kind: Service
metadata:
  name: mct-admin-dashboard
  namespace: inthewild
  labels:
    app: mct-admin-dashboard
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3006
    protocol: TCP
    name: http
  selector:
    app: mct-admin-dashboard
---
