apiVersion: v1
kind: Service
metadata:
  name: mct-kubernetes
  namespace: inthewild
  labels:
    app: mct-kubernetes
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3013
    protocol: TCP
    name: http
  selector:
    app: mct-kubernetes
---
