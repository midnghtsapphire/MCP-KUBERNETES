apiVersion: v1
kind: Service
metadata:
  name: mct-localization
  namespace: inthewild
  labels:
    app: mct-localization
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3022
    protocol: TCP
    name: http
  selector:
    app: mct-localization
---
