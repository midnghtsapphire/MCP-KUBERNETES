apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-admin-dashboard
  namespace: inthewild
  labels:
    app: mct-admin-dashboard
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-admin-dashboard
  template:
    metadata:
      labels:
        app: mct-admin-dashboard
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-admin-dashboard
        image: ghcr.io/midnghtsapphire/mct-admin-dashboard:latest
        ports:
        - containerPort: 3006
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3006"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 5
---
