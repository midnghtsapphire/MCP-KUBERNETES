apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-content-calendar
  namespace: inthewild
  labels:
    app: mct-content-calendar
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-content-calendar
  template:
    metadata:
      labels:
        app: mct-content-calendar
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-content-calendar
        image: ghcr.io/midnghtsapphire/mct-content-calendar:latest
        ports:
        - containerPort: 3019
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3019"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3019
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3019
          initialDelaySeconds: 10
          periodSeconds: 5
---
