apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-ai-chat
  namespace: inthewild
  labels:
    app: mct-ai-chat
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-ai-chat
  template:
    metadata:
      labels:
        app: mct-ai-chat
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-ai-chat
        image: ghcr.io/midnghtsapphire/mct-ai-chat:latest
        ports:
        - containerPort: 3009
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3009"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 10
          periodSeconds: 5
---
