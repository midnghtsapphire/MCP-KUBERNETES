apiVersion: v1
kind: Service
metadata:
  name: mct-affiliate
  namespace: inthewild
  labels:
    app: mct-affiliate
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: mct-affiliate
---
