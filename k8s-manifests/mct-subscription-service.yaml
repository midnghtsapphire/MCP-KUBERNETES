apiVersion: v1
kind: Service
metadata:
  name: mct-subscription
  namespace: inthewild
  labels:
    app: mct-subscription
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3004
    protocol: TCP
    name: http
  selector:
    app: mct-subscription
---
