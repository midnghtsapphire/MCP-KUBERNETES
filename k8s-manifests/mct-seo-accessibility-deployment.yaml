apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-seo-accessibility
  namespace: inthewild
  labels:
    app: mct-seo-accessibility
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-seo-accessibility
  template:
    metadata:
      labels:
        app: mct-seo-accessibility
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-seo-accessibility
        image: ghcr.io/midnghtsapphire/mct-seo-accessibility:latest
        ports:
        - containerPort: 3016
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3016"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3016
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3016
          initialDelaySeconds: 10
          periodSeconds: 5
---
