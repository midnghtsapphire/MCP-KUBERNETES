apiVersion: v1
kind: Service
metadata:
  name: mct-seo-accessibility
  namespace: inthewild
  labels:
    app: mct-seo-accessibility
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3016
    protocol: TCP
    name: http
  selector:
    app: mct-seo-accessibility
---
