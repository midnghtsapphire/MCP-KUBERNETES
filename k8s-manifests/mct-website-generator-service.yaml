apiVersion: v1
kind: Service
metadata:
  name: mct-website-generator
  namespace: inthewild
  labels:
    app: mct-website-generator
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3010
    protocol: TCP
    name: http
  selector:
    app: mct-website-generator
---
