apiVersion: v1
kind: Service
metadata:
  name: mct-ai-chat
  namespace: inthewild
  labels:
    app: mct-ai-chat
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3009
    protocol: TCP
    name: http
  selector:
    app: mct-ai-chat
---
