apiVersion: v1
kind: Service
metadata:
  name: mct-content-calendar
  namespace: inthewild
  labels:
    app: mct-content-calendar
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3019
    protocol: TCP
    name: http
  selector:
    app: mct-content-calendar
---
