apiVersion: v1
kind: Service
metadata:
  name: mct-email-marketing
  namespace: inthewild
  labels:
    app: mct-email-marketing
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3017
    protocol: TCP
    name: http
  selector:
    app: mct-email-marketing
---
