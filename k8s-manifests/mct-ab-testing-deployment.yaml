apiVersion: apps/v1
kind: Deployment
metadata:
  name: mct-ab-testing
  namespace: inthewild
  labels:
    app: mct-ab-testing
    tier: backend
    module: mct
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mct-ab-testing
  template:
    metadata:
      labels:
        app: mct-ab-testing
        tier: backend
        module: mct
    spec:
      containers:
      - name: mct-ab-testing
        image: ghcr.io/midnghtsapphire/mct-ab-testing:latest
        ports:
        - containerPort: 3020
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3020"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: database-url
        - name: OPENROUTER_API_KEY
          valueFrom:
            secretKeyRef:
              name: mct-secrets
              key: openrouter-api-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 10
          periodSeconds: 5
---
